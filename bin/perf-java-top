#!/bin/bash
set -e
#set -x

function usage() {
  echo "Usage:"
  echo "./perf-java-top <pid>"
  exit 1
}

if [ "$#" -ne 1 ]; then
  usage
fi

PERF_MAP_DIR=$(dirname $(readlink -f $0))/..

PID=$1
$PERF_MAP_DIR/bin/create-java-perf-map.sh $PID "$PERF_MAP_OPTIONS"
sudo perf top -p $*
